{
	// See https://go.microsoft.com/fwlink/?LinkId=733558
	// for the documentation about the tasks.json format
	"version": "2.0.0",
	"tasks": [
		{
			"label": "CMake build and run",
			"type": "shell",
			"command": "mkdir -p build && cd build && cmake .. && make -j 12 && ./rt",
			"group": {
				"kind": "test",
				"isDefault": true
			}
		}
	]
}